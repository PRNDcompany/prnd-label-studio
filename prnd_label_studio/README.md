# How to change label studio
- Implement code in `changes` directory with the same structure as the original label studio

# Notice
- Dockerfile (`prnd_label_studio/Dockerfile`) is copied from 
the original label studio (`prnd_label_studio/label-studio/Dockerfile`) and edited.
